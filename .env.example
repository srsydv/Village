GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
PORT=3001

# Public HTTPS origin after you deploy (no trailing slash).
# Used for Play AAB API calls and as the privacy-policy host.
PUBLIC_APP_URL=
CORS_ORIGINS=
